# @nola-lang/derive

Checker-backed schema derivation for Nola: the TypeScript walk that turns a resolved type into `__nola.types` combinator text, and the per-process language service the loader, the bundler plugins and `nola build`/`nola check` derive through. Internal to the toolchain; users never import it.
