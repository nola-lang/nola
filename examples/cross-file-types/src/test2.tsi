
type User = {
  id: string;
  name: string;
}

export infer function getUserById`get user from this userData`(.userData: string) {
  const userInfer = ask ..`get user from`<User>
  for (const user of userData) {
    
  }

  console.log(userInfer)
  return userInfer
}
