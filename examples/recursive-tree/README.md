# recursive-tree

Extracts a self-recursive `TreeNode` structure — the emit-contract-5 feature
demo: recursive types serialize as JSON Schema `$defs`/`$ref` and validate
recursively at run time. Runs against the mock provider.

```bash
nola run src/main.ts
```
